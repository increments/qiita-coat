{
  "name": "qiita-coat",
  "version": "0.0.0",
  "license": "MIT",
  "repository": "increments/qiita-coat",
  "homepage": "https://github.com/increments/qiita-coat",
  "scripts": {
    "build": "gulp",
    "build-pages": "bundle exec rspec",
    "deploy-pages": "gulp deploy",
    "sass": "gulp sass",
    "server": "rackup",
    "setup": "npm install && bundle install"
  },
  "devDependencies": {
    "gulp-gh-pages": "^0.5.2"
  }
}
