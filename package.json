{
  "name": "qiita-coat",
  "private": true,
  "scripts": {
    "build": "gulp",
    "build-pages": "bundle exec rspec",
    "deploy-pages": "gulp deploy",
    "sass": "gulp sass",
    "server": "rackup",
    "setup": "npm install && bundle install"
  },
  "devDependencies": {
    "gulp-gh-pages": "^0.5.2"
  }
}
