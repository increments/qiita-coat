{
  "name": "qiita-coat",
  "private": true,
  "scripts": {
    "build": "gulp",
    "deploy": "gulp deploy",
    "rspec": "bundle exec rspec",
    "sass": "gulp sass",
    "server": "rackup"
  },
  "devDependencies": {
    "gulp-gh-pages": "^0.5.2"
  }
}
