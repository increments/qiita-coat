main.main
  .main-inner
    .main-sub
      nav.menu
        a.menu-item[class=("menu-item-selected" if params[:document_id] == "grid") href=uri("/grid")]
          | Grid
        a.menu-item[class=("menu-item-selected" if params[:document_id] == "markdown") href=uri("/markdown")]
          | Markdown
    .main-main
      == slim :"docs/#{params[:document_id]}"
